<h1>Editing depth</h1>

<% form_for(@depth) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @depth %> |
<%= link_to 'Back', depths_path %>